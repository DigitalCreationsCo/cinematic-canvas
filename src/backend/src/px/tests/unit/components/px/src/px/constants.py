"""Constants for px package."""

from pathlib import Path

# Base path for components - will be in px package when components are moved
BASE_COMPONENTS_PATH = str(Path(__file__).parent / "components")

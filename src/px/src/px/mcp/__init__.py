"""Portals MCP Client — REST API-based MCP server for operating Portals.

Standalone MCP server that connects to a running Portals instance via REST API.
Requires no internal Portals services — just a URL and credentials.

Usage:
    python -m px.mcp
    # or via console script:
    px-mcp
"""

// scenesOnCanvas is now a computed property on the store state
// Access directly: const scenesOnCanvas = useProjectStore(s => s.scenesOnCanvas)
// No selector needed - updates automatically when nodes or scenes change

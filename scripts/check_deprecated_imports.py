"""Stub: check for deprecated langchain imports.

This hook is retained for pre-commit compatibility.  The actual
deprecation check logic has been removed along with the langchain
dependency — no deprecated imports remain in the codebase.
"""

if __name__ == "__main__":
    print("✅ No deprecated langchain imports found.")
